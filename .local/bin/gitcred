#!/bin/bash

if [ "$#" -ne 1 ]; then
    echo "Need 1 arg gitcred <work|home>"
fi

if [ "$1" = "home" ]; then
  echo "Setting up home git credentials"
  git config user.name "Jack Dallas"
  git config user.email "3620144+JackDallas@users.noreply.github.com"
elif [ "$1" = "work" ]; then
  echo "Setting up work git credentials"
  git config user.name "Jack Dallas"
  git config user.email "jack.dallas@vcatechnology.com"
else
  echo "Location $1 not found, args taken are <work|home>"
fi